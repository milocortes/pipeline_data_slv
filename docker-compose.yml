name: pipeline_data_slv
services:
  airflow-apiserver:
    command:
      - api-server
    depends_on:
      airflow-init:
        condition: service_completed_successfully
        required: true
      postgres:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      _PIP_ADDITIONAL_REQUIREMENTS: apache-airflow-providers-docker
      AIRFLOW__API__SECRET_KEY: bar
      AIRFLOW__API_AUTH__JWT_SECRET: foo
      AIRFLOW__CELERY__BROKER_URL: redis://:@redis:6379/0
      AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://airflow:airflow@postgres/airflow
      AIRFLOW__CORE__AUTH_MANAGER: airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager
      AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
      AIRFLOW__CORE__EXECUTION_API_SERVER_URL: http://airflow-apiserver:8080/execution/
      AIRFLOW__CORE__EXECUTOR: CeleryExecutor
      AIRFLOW__CORE__FERNET_KEY: ""
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__CORE__TEST_CONNECTION: Enabled
      AIRFLOW__DAG_PROCESSOR__REFRESH_INTERVAL: "60"
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres/airflow
      AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: "true"
      AIRFLOW_CONFIG: /opt/airflow/config/airflow.cfg
      AIRFLOW_GID: "0"
      AIRFLOW_IMAGE_NAME: apache/airflow:3.1.2
      AIRFLOW_UID: "1000"
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: "true"
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_SERVER_DOMAINS: example.com
    healthcheck:
      test:
        - CMD
        - curl
        - --fail
        - http://localhost:8080/api/v2/version
      timeout: 10s
      interval: 30s
      retries: 5
      start_period: 30s
    image: apache/airflow:3.1.2
    networks:
      default: null
    ports:
      - mode: ingress
        target: 8080
        published: "8080"
        protocol: tcp
    restart: always
    user: "1000:0"
    volumes:
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/dags
        target: /opt/airflow/dags
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/logs
        target: /opt/airflow/logs
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/config
        target: /opt/airflow/config
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/plugins
        target: /opt/airflow/plugins
        bind: {}
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
        bind: {}
      - type: volume
        source: airflow-data-volume
        target: /data
        volume: {}
  airflow-dag-processor:
    command:
      - dag-processor
    depends_on:
      airflow-init:
        condition: service_completed_successfully
        required: true
      postgres:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      _PIP_ADDITIONAL_REQUIREMENTS: apache-airflow-providers-docker
      AIRFLOW__API__SECRET_KEY: bar
      AIRFLOW__API_AUTH__JWT_SECRET: foo
      AIRFLOW__CELERY__BROKER_URL: redis://:@redis:6379/0
      AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://airflow:airflow@postgres/airflow
      AIRFLOW__CORE__AUTH_MANAGER: airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager
      AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
      AIRFLOW__CORE__EXECUTION_API_SERVER_URL: http://airflow-apiserver:8080/execution/
      AIRFLOW__CORE__EXECUTOR: CeleryExecutor
      AIRFLOW__CORE__FERNET_KEY: ""
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__CORE__TEST_CONNECTION: Enabled
      AIRFLOW__DAG_PROCESSOR__REFRESH_INTERVAL: "60"
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres/airflow
      AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: "true"
      AIRFLOW_CONFIG: /opt/airflow/config/airflow.cfg
      AIRFLOW_GID: "0"
      AIRFLOW_IMAGE_NAME: apache/airflow:3.1.2
      AIRFLOW_UID: "1000"
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: "true"
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_SERVER_DOMAINS: example.com
    healthcheck:
      test:
        - CMD-SHELL
        - airflow jobs check --job-type DagProcessorJob --hostname "$${HOSTNAME}"
      timeout: 10s
      interval: 30s
      retries: 5
      start_period: 30s
    image: apache/airflow:3.1.2
    networks:
      default: null
    restart: always
    user: "1000:0"
    volumes:
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/dags
        target: /opt/airflow/dags
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/logs
        target: /opt/airflow/logs
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/config
        target: /opt/airflow/config
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/plugins
        target: /opt/airflow/plugins
        bind: {}
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
        bind: {}
      - type: volume
        source: airflow-data-volume
        target: /data
        volume: {}
  airflow-data:
    command:
      - -c
      - |
        if [[ -z "1000" ]]; then
          echo
          echo -e "\033[1;33mWARNING!!!: AIRFLOW_UID not set!\e[0m"
          echo "If you are on Linux, you SHOULD follow the instructions below to set "
          echo "AIRFLOW_UID environment variable, otherwise files will be owned by root."
          echo "For other operating systems you can get rid of the warning with manually created .env file:"
          echo "    See: https://airflow.apache.org/docs/apache-airflow/stable/howto/docker-compose/index.html#setting-the-right-airflow-user"
          echo
        fi
        chown -R "1000:0" /data
        exec /entrypoint airflow version
    entrypoint:
      - /bin/bash
    environment:
      AIRFLOW__API__SECRET_KEY: bar
      AIRFLOW__API_AUTH__JWT_SECRET: foo
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__CORE__TEST_CONNECTION: Enabled
      AIRFLOW__DAG_PROCESSOR__REFRESH_INTERVAL: "60"
    image: apache/airflow:3.1.2
    networks:
      default: null
    user: "0:0"
    volumes:
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
        bind: {}
      - type: volume
        source: airflow-data-volume
        target: /data
        volume: {}
  airflow-init:
    command:
      - -c
      - |
        if [[ -z "1000" ]]; then
          echo
          echo -e "\033[1;33mWARNING!!!: AIRFLOW_UID not set!\e[0m"
          echo "If you are on Linux, you SHOULD follow the instructions below to set "
          echo "AIRFLOW_UID environment variable, otherwise files will be owned by root."
          echo "For other operating systems you can get rid of the warning with manually created .env file:"
          echo "    See: https://airflow.apache.org/docs/apache-airflow/stable/howto/docker-compose/index.html#setting-the-right-airflow-user"
          echo
          export AIRFLOW_UID=$$(id -u)
        fi
        one_meg=1048576
        mem_available=$$(($$(getconf _PHYS_PAGES) * $$(getconf PAGE_SIZE) / one_meg))
        cpus_available=$$(grep -cE 'cpu[0-9]+' /proc/stat)
        disk_available=$$(df / | tail -1 | awk '{print $$4}')
        warning_resources="false"
        if (( mem_available < 4000 )) ; then
          echo
          echo -e "\033[1;33mWARNING!!!: Not enough memory available for Docker.\e[0m"
          echo "At least 4GB of memory required. You have $$(numfmt --to iec $$((mem_available * one_meg)))"
          echo
          warning_resources="true"
        fi
        if (( cpus_available < 2 )); then
          echo
          echo -e "\033[1;33mWARNING!!!: Not enough CPUS available for Docker.\e[0m"
          echo "At least 2 CPUs recommended. You have $${cpus_available}"
          echo
          warning_resources="true"
        fi
        if (( disk_available < one_meg * 10 )); then
          echo
          echo -e "\033[1;33mWARNING!!!: Not enough Disk space available for Docker.\e[0m"
          echo "At least 10 GBs recommended. You have $$(numfmt --to iec $$((disk_available * 1024 )))"
          echo
          warning_resources="true"
        fi
        if [[ $${warning_resources} == "true" ]]; then
          echo
          echo -e "\033[1;33mWARNING!!!: You have not enough resources to run Airflow (see above)!\e[0m"
          echo "Please follow the instructions to increase amount of resources available:"
          echo "   https://airflow.apache.org/docs/apache-airflow/stable/howto/docker-compose/index.html#before-you-begin"
          echo
        fi
        echo
        echo "Creating missing opt dirs if missing:"
        echo
        mkdir -v -p /opt/airflow/{logs,dags,plugins,config}
        echo
        echo "Airflow version:"
        /entrypoint airflow version
        echo
        echo "Files in shared volumes:"
        echo
        ls -la /opt/airflow/{logs,dags,plugins,config}
        echo
        echo "Running airflow config list to create default config file if missing."
        echo
        /entrypoint airflow config list >/dev/null
        echo
        echo "Files in shared volumes:"
        echo
        ls -la /opt/airflow/{logs,dags,plugins,config}
        echo
        echo "Change ownership of files in /opt/airflow to 1000:0"
        echo
        chown -R "1000:0" /opt/airflow/
        echo
        echo "Change ownership of files in shared volumes to 1000:0"
        echo
        chown -v -R "1000:0" /opt/airflow/{logs,dags,plugins,config}
        echo
        echo "Files in shared volumes:"
        echo
        ls -la /opt/airflow/{logs,dags,plugins,config}
    depends_on:
      postgres:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    entrypoint:
      - /bin/bash
    environment:
      _AIRFLOW_DB_MIGRATE: "true"
      _AIRFLOW_WWW_USER_CREATE: "true"
      _AIRFLOW_WWW_USER_PASSWORD: airflow
      _AIRFLOW_WWW_USER_USERNAME: airflow
      _PIP_ADDITIONAL_REQUIREMENTS: ""
      AIRFLOW__API__SECRET_KEY: bar
      AIRFLOW__API_AUTH__JWT_SECRET: foo
      AIRFLOW__CELERY__BROKER_URL: redis://:@redis:6379/0
      AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://airflow:airflow@postgres/airflow
      AIRFLOW__CORE__AUTH_MANAGER: airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager
      AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
      AIRFLOW__CORE__EXECUTION_API_SERVER_URL: http://airflow-apiserver:8080/execution/
      AIRFLOW__CORE__EXECUTOR: CeleryExecutor
      AIRFLOW__CORE__FERNET_KEY: ""
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__CORE__TEST_CONNECTION: Enabled
      AIRFLOW__DAG_PROCESSOR__REFRESH_INTERVAL: "60"
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres/airflow
      AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: "true"
      AIRFLOW_CONFIG: /opt/airflow/config/airflow.cfg
      AIRFLOW_GID: "0"
      AIRFLOW_IMAGE_NAME: apache/airflow:3.1.2
      AIRFLOW_UID: "1000"
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: "true"
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_SERVER_DOMAINS: example.com
    image: apache/airflow:3.1.2
    networks:
      default: null
    user: "0:0"
    volumes:
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/dags
        target: /opt/airflow/dags
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/logs
        target: /opt/airflow/logs
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/config
        target: /opt/airflow/config
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/plugins
        target: /opt/airflow/plugins
        bind: {}
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
        bind: {}
      - type: volume
        source: airflow-data-volume
        target: /data
        volume: {}
  airflow-scheduler:
    command:
      - scheduler
    depends_on:
      airflow-init:
        condition: service_completed_successfully
        required: true
      postgres:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      _PIP_ADDITIONAL_REQUIREMENTS: apache-airflow-providers-docker
      AIRFLOW__API__SECRET_KEY: bar
      AIRFLOW__API_AUTH__JWT_SECRET: foo
      AIRFLOW__CELERY__BROKER_URL: redis://:@redis:6379/0
      AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://airflow:airflow@postgres/airflow
      AIRFLOW__CORE__AUTH_MANAGER: airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager
      AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
      AIRFLOW__CORE__EXECUTION_API_SERVER_URL: http://airflow-apiserver:8080/execution/
      AIRFLOW__CORE__EXECUTOR: CeleryExecutor
      AIRFLOW__CORE__FERNET_KEY: ""
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__CORE__TEST_CONNECTION: Enabled
      AIRFLOW__DAG_PROCESSOR__REFRESH_INTERVAL: "60"
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres/airflow
      AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: "true"
      AIRFLOW_CONFIG: /opt/airflow/config/airflow.cfg
      AIRFLOW_GID: "0"
      AIRFLOW_IMAGE_NAME: apache/airflow:3.1.2
      AIRFLOW_UID: "1000"
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: "true"
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_SERVER_DOMAINS: example.com
    healthcheck:
      test:
        - CMD
        - curl
        - --fail
        - http://localhost:8974/health
      timeout: 10s
      interval: 30s
      retries: 5
      start_period: 30s
    image: apache/airflow:3.1.2
    networks:
      default: null
    restart: always
    user: "1000:0"
    volumes:
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/dags
        target: /opt/airflow/dags
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/logs
        target: /opt/airflow/logs
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/config
        target: /opt/airflow/config
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/plugins
        target: /opt/airflow/plugins
        bind: {}
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
        bind: {}
      - type: volume
        source: airflow-data-volume
        target: /data
        volume: {}
  airflow-triggerer:
    command:
      - triggerer
    depends_on:
      airflow-init:
        condition: service_completed_successfully
        required: true
      postgres:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      _PIP_ADDITIONAL_REQUIREMENTS: apache-airflow-providers-docker
      AIRFLOW__API__SECRET_KEY: bar
      AIRFLOW__API_AUTH__JWT_SECRET: foo
      AIRFLOW__CELERY__BROKER_URL: redis://:@redis:6379/0
      AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://airflow:airflow@postgres/airflow
      AIRFLOW__CORE__AUTH_MANAGER: airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager
      AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
      AIRFLOW__CORE__EXECUTION_API_SERVER_URL: http://airflow-apiserver:8080/execution/
      AIRFLOW__CORE__EXECUTOR: CeleryExecutor
      AIRFLOW__CORE__FERNET_KEY: ""
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__CORE__TEST_CONNECTION: Enabled
      AIRFLOW__DAG_PROCESSOR__REFRESH_INTERVAL: "60"
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres/airflow
      AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: "true"
      AIRFLOW_CONFIG: /opt/airflow/config/airflow.cfg
      AIRFLOW_GID: "0"
      AIRFLOW_IMAGE_NAME: apache/airflow:3.1.2
      AIRFLOW_UID: "1000"
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: "true"
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_SERVER_DOMAINS: example.com
    healthcheck:
      test:
        - CMD-SHELL
        - airflow jobs check --job-type TriggererJob --hostname "$${HOSTNAME}"
      timeout: 10s
      interval: 30s
      retries: 5
      start_period: 30s
    image: apache/airflow:3.1.2
    networks:
      default: null
    restart: always
    user: "1000:0"
    volumes:
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/dags
        target: /opt/airflow/dags
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/logs
        target: /opt/airflow/logs
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/config
        target: /opt/airflow/config
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/plugins
        target: /opt/airflow/plugins
        bind: {}
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
        bind: {}
      - type: volume
        source: airflow-data-volume
        target: /data
        volume: {}
  airflow-worker:
    command:
      - celery
      - worker
    depends_on:
      airflow-apiserver:
        condition: service_healthy
        required: true
      airflow-init:
        condition: service_completed_successfully
        required: true
      postgres:
        condition: service_healthy
        required: true
      redis:
        condition: service_healthy
        required: true
    environment:
      _PIP_ADDITIONAL_REQUIREMENTS: apache-airflow-providers-docker
      AIRFLOW__API__SECRET_KEY: bar
      AIRFLOW__API_AUTH__JWT_SECRET: foo
      AIRFLOW__CELERY__BROKER_URL: redis://:@redis:6379/0
      AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://airflow:airflow@postgres/airflow
      AIRFLOW__CORE__AUTH_MANAGER: airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager
      AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
      AIRFLOW__CORE__EXECUTION_API_SERVER_URL: http://airflow-apiserver:8080/execution/
      AIRFLOW__CORE__EXECUTOR: CeleryExecutor
      AIRFLOW__CORE__FERNET_KEY: ""
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__CORE__TEST_CONNECTION: Enabled
      AIRFLOW__DAG_PROCESSOR__REFRESH_INTERVAL: "60"
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres/airflow
      AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: "true"
      AIRFLOW_CONFIG: /opt/airflow/config/airflow.cfg
      AIRFLOW_GID: "0"
      AIRFLOW_IMAGE_NAME: apache/airflow:3.1.2
      AIRFLOW_UID: "1000"
      DUMB_INIT_SETSID: "0"
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: "true"
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_SERVER_DOMAINS: example.com
    healthcheck:
      test:
        - CMD-SHELL
        - celery --app airflow.providers.celery.executors.celery_executor.app inspect ping -d "celery@$${HOSTNAME}" || celery --app airflow.executors.celery_executor.app inspect ping -d "celery@$${HOSTNAME}"
      timeout: 10s
      interval: 30s
      retries: 5
      start_period: 30s
    image: apache/airflow:3.1.2
    networks:
      default: null
    restart: always
    user: "1000:0"
    volumes:
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/dags
        target: /opt/airflow/dags
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/logs
        target: /opt/airflow/logs
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/config
        target: /opt/airflow/config
        bind: {}
      - type: bind
        source: /home/milo/Documents/egtp/iniciativas/pipeline_data_slv/plugins
        target: /opt/airflow/plugins
        bind: {}
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
        bind: {}
      - type: volume
        source: airflow-data-volume
        target: /data
        volume: {}
  postgres:
    environment:
      POSTGRES_DB: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_USER: airflow
    healthcheck:
      test:
        - CMD
        - pg_isready
        - -U
        - airflow
      interval: 10s
      retries: 5
      start_period: 5s
    image: postgres:16
    networks:
      default: null
    restart: always
    volumes:
      - type: volume
        source: postgres-db-volume
        target: /var/lib/postgresql/data
        volume: {}
  redis:
    expose:
      - "6379"
    healthcheck:
      test:
        - CMD
        - redis-cli
        - ping
      timeout: 30s
      interval: 10s
      retries: 50
      start_period: 30s
    image: redis:7.2-bookworm
    networks:
      default: null
    restart: always
  rustfs:
    command:
      - --address
      - :9000
      - --console-enable
      - --server-domains
      - example.com
      - --access-key
      - rustfsadmin
      - --secret-key
      - rustfsadmin
      - /data
    container_name: rustfs-storage
    environment:
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: "true"
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_SERVER_DOMAINS: example.com
    image: rustfs/rustfs:latest
    networks:
      default: null
    ports:
      - mode: ingress
        target: 9000
        published: "9000"
        protocol: tcp
      - mode: ingress
        target: 9001
        published: "9001"
        protocol: tcp
    volumes:
      - type: bind
        source: /mnt/rustfs/data
        target: /data
        bind: {}
networks:
  default:
    name: pipeline_data_slv_default
volumes:
  airflow-data-volume:
    name: pipeline_data_slv_airflow-data-volume
  postgres-db-volume:
    name: pipeline_data_slv_postgres-db-volume
x-airflow-common:
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_healthy
  env_file:
    - .env
  environment:
    _PIP_ADDITIONAL_REQUIREMENTS: apache-airflow-providers-docker
    AIRFLOW__API__SECRET_KEY: bar
    AIRFLOW__API_AUTH__JWT_SECRET: foo
    AIRFLOW__CELERY__BROKER_URL: redis://:@redis:6379/0
    AIRFLOW__CELERY__RESULT_BACKEND: db+postgresql://airflow:airflow@postgres/airflow
    AIRFLOW__CORE__AUTH_MANAGER: airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager
    AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
    AIRFLOW__CORE__EXECUTION_API_SERVER_URL: http://airflow-apiserver:8080/execution/
    AIRFLOW__CORE__EXECUTOR: CeleryExecutor
    AIRFLOW__CORE__FERNET_KEY: ""
    AIRFLOW__CORE__LOAD_EXAMPLES: "false"
    AIRFLOW__CORE__TEST_CONNECTION: Enabled
    AIRFLOW__DAG_PROCESSOR__REFRESH_INTERVAL: 60
    AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres/airflow
    AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: "true"
    AIRFLOW_CONFIG: /opt/airflow/config/airflow.cfg
  image: apache/airflow:3.1.2
  user: "1000:0"
  volumes:
    - ./dags:/opt/airflow/dags
    - ./logs:/opt/airflow/logs
    - ./config:/opt/airflow/config
    - ./plugins:/opt/airflow/plugins
    - /var/run/docker.sock:/var/run/docker.sock
    - airflow-data-volume:/data
